class NotePresentation {
  final int id;
  final String title, description;
  final DateTime createdAt;

  NotePresentation(
    this.id,
    this.title,
    this.description,
    this.createdAt,
  );
}
